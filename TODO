- import <bundle_id> data dir into data/
- reuse the same data dir for all SN names for the same context
- treat "/" in user datain requests
- GC and stale fs cache
- stale Object.cache_props and _missed_blobs
- empty stub for not accessible SN server should be replaced by local SN db
- synchronize local "keep" resources
- optimize getting/setting(?) StoredProperty values for home folder
- handle reconnects for regular remote connections and subscriptions
- (?) delete local document on unsetting keep/keep_impl
